import React from "react";

const AttendStatus = () => {
  return (
    <div
      style={{
        width: "100%",
        height: "730px",
        background: "#fff",
        borderRadius: "5px",
        padding: "20px 45px",
        boxShadow: "0px 0.5px 5px 0px rgba(0, 0, 0, 0.2)",
      }}
    >
      AttendStatus
    </div>
  );
};

export default AttendStatus;
